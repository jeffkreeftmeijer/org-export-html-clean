Clean HTML for Org-Mode Export
==============================

This package provides cleaner (in my humble opinion) HTML output than Org-Mode's
default output.

## Features

* No file extensions: `my-file.org` is published to `http://example.com/my-file/`
